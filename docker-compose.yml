# ============================================================================
# DEVELOPMENT infrastructure only — Postgres, MinIO, Mailpit.
# Started by `bun run setup` / `bun run dev`; the app itself runs on the host
# via `bun run dev`, so there is NO app service here.
#
# This file uses insecure defaults (postgres/password, minioadmin), publishes
# datastore ports on localhost, and makes the upload bucket world-readable —
# all fine for local development, NOT safe on an internet-facing host.
#
# To self-host Quackback, use docker-compose.prod.yml instead (app included,
# hardened defaults). See the self-hosting docs.
# ============================================================================

services:
  postgres:
    build:
      context: ./docker/postgres
      dockerfile: Dockerfile
    container_name: quackback-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: quackback
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./docker/postgres/init-pgcron.sql:/docker-entrypoint-initdb.d/init-pgcron.sql
    command: postgres -c shared_preload_libraries=pg_cron -c cron.database_name=quackback -c max_connections=200
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    # PGSTY Silo, the maintained community fork of MinIO (MinIO itself is
    # archived and no registry serves its images any more). Same S3 API,
    # `MINIO_*` variables and on-disk format; the client is `mcli`. Pinned to
    # the release the prod stacks run.
    image: docker.io/pgsty/silo:RELEASE.2026-09-16T00-00-00Z
    container_name: quackback-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ['CMD', 'mcli', 'ready', 'local']
      interval: 5s
      timeout: 5s
      retries: 5

  mailpit:
    image: axllent/mailpit:latest
    container_name: quackback-mailpit
    restart: unless-stopped
    ports:
      - '8025:8025' # Web UI
      - '1025:1025' # SMTP
    healthcheck:
      test: ['CMD', 'wget', '--spider', '-q', 'http://localhost:8025/api/v1/info']
      interval: 5s
      timeout: 5s
      retries: 5

  # Creates the bucket and sets a public download policy on startup. Runs
  # from the server image, which bundles the `mcli` client.
  minio-init:
    image: docker.io/pgsty/silo:RELEASE.2026-09-16T00-00-00Z
    container_name: quackback-minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mcli alias set local http://minio:9000 minioadmin minioadmin;
      mcli mb local/quackback --ignore-existing;
      mcli anonymous set download local/quackback;
      exit 0;
      "
    restart: 'no'

volumes:
  postgres_data:
  minio_data:
